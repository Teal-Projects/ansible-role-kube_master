kube_version: 1.17.3
kubernetes_url: https://storage.googleapis.com/kubernetes-release/release/v{{ kube_version }}/kubernetes-server-linux-amd64.tar.gz

# api-server
etcd_servers: 
portal_net: 10.200.0.0/16
api_servers: '{{ ansible_default_ipv4.address }}'
kube_token: 

